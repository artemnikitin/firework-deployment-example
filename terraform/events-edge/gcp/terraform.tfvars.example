gcp_project   = "my-gcp-project"
gcp_region    = "us-central1"
dns_zone_name = "firework-gcp"
events_domain = "events.gcp.example.com"

# Keep this name stable when creating and destroying control planes. The
# certificate, certificate map, DNS authorization, and IP are intentionally
# shared durable infrastructure.
# edge_name = "firework-events-edge"
